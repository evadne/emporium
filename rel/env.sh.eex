#!/usr/bin/env bash

if [ -z "${NODE_NAME+x}" ]; then
  echo "NODE_NAME not configured."
  export RELEASE_DISTRIBUTION="none"
else
  export RELEASE_DISTRIBUTION="name"
  export RELEASE_NODE="$NODE_NAME"

  if [ ! -z "${NODE_COOKIE+x}" ]; then
    export RELEASE_COOKIE="$NODE_COOKIE"
  fi
fi
